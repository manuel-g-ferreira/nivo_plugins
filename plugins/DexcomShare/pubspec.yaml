name: dexcomshare_plugin
description: Dexcom CGM plugin for Nivo via Dexcom Share (JSON line protocol v1).
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.5.0

dependencies:

dev_dependencies:
  test: ^1.25.0