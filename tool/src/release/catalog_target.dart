/// Default public catalog repository published by nivo-plugins release CI.
const defaultCatalogRepo = 'manuel-g-ferreira/nivo-plugin-catalog';

const defaultCatalogBranch = 'main';

const defaultCatalogDistDir = 'dist';

/// Platforms produced by the release build matrix (macOS arm64, Windows, Linux).
const releasePlatformIds = [
  'darwin-arm64',
  'windows-x64',
  'linux-x64',
];
